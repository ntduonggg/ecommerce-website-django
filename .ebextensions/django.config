option_settings:
  # --- WSGI configuration ---
  aws:elasticbeanstalk:container:python:
    WSGIPath: webapp/wsgi:application

  # --- Proxy static files mapping ---
  aws:elasticbeanstalk:environment:proxy:staticfiles:
    /static: static